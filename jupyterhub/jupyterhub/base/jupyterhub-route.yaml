apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: jupyterhub
    type: sharded
  name: jupyterhub
spec:
  host: jupyterhub-ppe-dha-data-poc.dhanonprod.apps-nonprod.nabocpclu01.dohms.gov.ae
  port:
    targetPort: 8080-tcp
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: jupyterhub
    weight: 100
